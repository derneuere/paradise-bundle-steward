// Surviving per-challenge UI blocks — wrapped by the schema-editor
// extensions at src/components/schema-editor/extensions/challengeListExtensions.tsx.
// The old `ChallengeListEditor` / `ChallengeEntriesList` / `ChallengeEntryEditor`
// wrappers were removed once the schema editor replaced the top-level
// tab layout and the virtualized challenge list.
export { ChallengeOverview } from './ChallengeOverview';
export { ActionEditor } from './ActionEditor';
export { LocationDataEditor } from './LocationDataEditor';
